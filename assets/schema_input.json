{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/metaamrplus/master/assets/schema_input.json",
    "title": "nf-core/metaamrplus pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^[a-zA-Z0-9_-]+$",
                "errorMessage": "Sample name must be provided and can only contain letters, numbers, hyphens and underscores (no spaces or special characters)",
                "meta": ["id"]
            },
            "reads": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.f(ast)?q\\.gz$",
                "errorMessage": "Nanopore FASTQ file must be provided, cannot contain spaces and must have extension '.fq.gz' or '.fastq.gz'"
            }
        },
        "required": ["sample", "reads"]
    }
}
